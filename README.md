# Дашборд менеджеров

Файл запуска: `index.html`.

## Логины

| Роль | Логин | Пароль |
|---|---|---|
| Руководитель | `ruk` | `Ruk-4821` |
| Мартынов А | `martynov` | `Mar-7392` |
| Александров С | `alexandrov` | `Alex-5168` |
| Синчагова С | `sinchagova` | `Sin-9047` |

## Источник данных

Google Таблица: лист `реестр (копия)`.

Кнопка `Обновить данные` загружает CSV с Google Таблицы и пересчитывает показатели в браузере. Если загрузка недоступна, используется локальный снимок `data.js`.

## План

Планы берутся из техлистов:

| Менеджер | Лист | План заявок | План поступлений |
|---|---|---|---|
| Мартынов А | `тех лист` | колонка `H` | колонка `D` |
| Александров С | `Александров ` | колонка `C` | колонка `E` |
| Синчагова С | `Синчагова С` | колонка `C` | колонка `E` |

Формула выполнения: `Факт поступлений / План поступлений`.
